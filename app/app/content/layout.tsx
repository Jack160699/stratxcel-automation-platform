import type { ReactNode } from "react";

/**
 * Customer Content & Media Layout — canonical workspace shell for content library,
 * creatives, studio, calendar, and publishing pipelines.
 */
export default function ContentLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
