-- Additive capability_registry row for check_domain_status. Applied live
-- via Supabase MCP on 2026-09-02; this file makes that reproducible from
-- a fresh database.
insert into public.capability_registry
  (capability_key, name, description, category, skill, agent_tool_name, package_or_module, department, connection, required_permission, read_write, tenant_scope, cost_profile, risk, verification_method, status, status_notes, external_blocker, last_verified_at, last_verified_by)
values
  ('agent_tool:check_domain_status', 'Real live domain DNS + Vercel verification status', 'Real public DNS records (A/CNAME/AAAA/nameservers, safe read-only resolution) via inspectDomainDns, plus Vercel''s own verified/sslActive state via getVercelDomainStatus -- same VERCEL_AUTH_TOKEN/project/team defaults execute_growth_action already uses. Complements check_website_status (which only reads the stored custom_domain value, not its live state). Live-verified this pass: real DNS resolution proven against stratxcel.in (real A record, real nameservers returned); Vercel status proven to degrade gracefully without a token, success path already proven live via execute_growth_action.', 'website', 'domain status read', 'check_domain_status', 'packages/websites-and-domains (inspectDomainDns, getVercelDomainStatus) via lib/agent-core/growth-media-tools.ts', 'Engineering', 'vercel', 'agent:read:website', 'read', 'global', 'free', 'read', 'none (read-only; real DNS resolution smoke-tested live against stratxcel.in before shipping)', 'REAL_EXPOSED', 'Answers mission section 11''s "domain operations where legitimately supported" without touching the creation/editing/deployment mutation surface, which correctly stays REAL_NOT_EXPOSED (engine:website_vercel_orchestration).', null, now(), 'claude_session_2026-09-02');
