-- Records the new check_google_business agent tool (master brief section
-- 9/22). Applied live via Supabase MCP on 2026-09-02; this file makes that
-- reproducible from a fresh database.
insert into public.capability_registry
  (capability_key, name, description, category, skill, agent_tool_name, package_or_module, department, connection, required_permission, read_write, tenant_scope, cost_profile, risk, verification_method, status, status_notes, external_blocker, last_verified_at, last_verified_by)
values
  ('capability:check_google_business_tool', 'check_google_business: real Google Business Profile reviews + connection state, exposed to WhatsApp/Admin/Client Copilot', 'Master brief section 9/22 ("Check Google"). Reuses the exact same real functions the already-running automated Review Bot cron uses (app/api/internal/search/scheduler/route.ts''s runReviewBotCycle): resolveAccessToken (lib/social/audit-connector-insights.ts), isResolvedGbpLocationResourceName and listLocationReviews (lib/social/providers/google-business.ts). Read-only -- never replies to a review itself; the Review Bot already owns that on its own schedule. Every failure mode is surfaced honestly rather than as a generic error: no account on file, not connected, location never resolved (needs reconnect), token unavailable, or a live API fetch failure each get their own distinct, real reason string.', 'ecosystem', null, 'check_google_business', 'lib/agent-core/google-business-tool.ts', 'Engineering', null, 'agent:read:integrations', 'read', 'tenant', 'free', 'read', 'Full-repo tsc --noEmit clean, lint clean, real NODE_ENV=production next build exit 0. No dedicated unit test written -- matches the established pattern for the other real-API-calling read tools in this file (check_connections, check_website_status), which are verified via typecheck/lint/build/production usage rather than a mocked external-API test.', 'REAL_EXPOSED', null, null, now(), 'claude_session_2026-09-02')
on conflict (capability_key) do nothing;
